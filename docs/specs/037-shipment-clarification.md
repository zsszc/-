# Spec 037：缺少运单号时的 SLA 边界澄清

## 状态

已批准，进入实现阶段。

## 目标

优化“清关异常 + SLA 判断”但未提供运单号时的回复：明确说明需要运单号，避免泛化拒答或虚构轨迹查询。

## 范围

- 识别包含清关/超时/SLA 等状态核查诉求且没有运单号的知识路由兜底。
- 返回可执行的运单号补充提示。
- 评测中将“缺少必要参数后的正确澄清”视为跨文档问题的合格行为。

## 验收标准

- 缺少运单号时不调用查询工具、不编造状态，并明确要求提供运单号。
- 普通知识库无关问题继续使用原有拒答文案。
- 新增定向测试通过，Spec 回填实现记录并提交推送。

## 实现记录

- 已新增缺少运单号时的 SLA/清关定向澄清文案和状态 trace。
- 已让评测把缺少关键参数后的正确澄清视为合格行为。
- 已补充 `tests/test_shipment_clarification.py`。
